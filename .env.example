# Google Analytics 4 Measurement Protocol API Secret
# Required for analytics to function. Obtain from GA4 Admin > Data Streams > Measurement Protocol API secrets.
# This value is injected at build time via Vite's import.meta.env replacement.
VITE_GA4_API_SECRET=
